<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VirtualDeputy"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <!-- Splash / Router -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Главный экран (будет чатом в Части 2) -->
        <activity android:name=".HomeActivity" />

        <!-- Авторизация -->
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".auth.ResetPasswordActivity" />
        <activity android:name=".auth.TermsActivity" />

        <!-- Чат -->
        <activity android:name=".chat.ChatActivity" />

        <!-- История -->
        <activity android:name=".history.HistoryActivity" />

        <!-- Аналитика заявлений -->
        <!-- intent-filter для FCM click_action: при тапе на уведомление
             система открывает AnalyticsActivity напрямую, передавая data-extras -->
        <activity
            android:name=".analytics.AnalyticsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="OPEN_ANALYTICS_ACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Профиль -->
        <activity android:name=".profile.ProfileActivity" />

        <!-- Живой чат с ИИ — Light-тема с белым windowBackground:
             окно создаётся белым ещё до старта анимаций → нет чёрного артефакта.
             singleTop — одна инстанция в стеке; FLAG_ACTIVITY_CLEAR_TOP из ChatActivity
             гарантирует, что повторный тап поднимает её наверх, а recreate() корректно
             применяет смену языка при каждом изменении. -->
        <activity
            android:name=".live.LiveChatActivity"
            android:theme="@style/Theme.VirtualDeputy.LiveChat"
            android:windowSoftInputMode="adjustPan"
            android:launchMode="singleTop" />

        <!-- FileProvider для камеры (передача URI снимка) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- FCM: получение токенов и входящих уведомлений -->
        <service
            android:name=".fcm.AppFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
